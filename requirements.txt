# HandsOff (再买剁手) — runtime dependencies.
# Python >= 3.11 (developed and tested on 3.13).
#
# Async HTTP client for Helius RPC + DexScreener AND the aiohttp web server.
# Persistence uses the stdlib `sqlite3`; config uses the stdlib `configparser`;
# the soft/hard reminder is spoken in the BROWSER via the Web Speech API — so no
# Python audio / TTS dependency is needed.
aiohttp>=3.11.0,<4.0.0

# Solana wallet login for the web UI — ONLY needed when [WEB] enable_auth = true:
#   pynacl -> Ed25519 signature verification
#   base58 -> decode wallet addresses / signatures
# Safe to omit entirely if you keep the UI open on 127.0.0.1 (enable_auth = false).
pynacl>=1.5,<2.0.0
base58>=2.1,<3.0.0
